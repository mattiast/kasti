version: 2
jobs:
  build:
    docker:
      - image: mattiast/kasti-build:latest
    steps:
      - checkout
      - restore_cache:
          key: stack_dir2
      - restore_cache:
          key: elm-stuff2
      - run:
          name: Build stuff
          command: make
      - save_cache:
          paths:
            - /home/circleci/.stack
          key: stack_dir2-{{ epoch }}
      - save_cache:
          paths:
            - frontend/elm-stuff
          key: elm-stuff2-{{ epoch }}
      - store_artifacts:
          path: stuff.tar.gz
